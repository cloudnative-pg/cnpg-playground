#!/usr/bin/env bash
# No-op stand-in for `kubectl`, used only to satisfy prerequisite checks in
# common.sh/demo/setup.sh during DRY_RUN template-rendering tests, which never
# actually invoke kubectl.
exit 0

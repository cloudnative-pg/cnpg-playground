apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: objectstore-us
spec:
  configuration:
    destinationPath: s3://backups/
    endpointURL: http://objectstore-us:9000
    s3Credentials:
      accessKeyId:
        name: objectstore-us
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: objectstore-us
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip

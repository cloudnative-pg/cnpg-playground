apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: pg-${REGION}-podmonitor
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: pg-${REGION}
  podMetricsEndpoints:
  - port: metrics
